# =============================================================================
#  content-creation — variables d'environnement
#  Copie ce fichier en .env puis remplis les valeurs :
#     cp .env.example .env
#  Les secrets se génèrent avec :  openssl rand -hex 32
# =============================================================================

# Domaine de base (utilisé pour toutes les routes + portail Authelia)
DOMAIN=content-creation.example.com

# --- Secrets Authelia (openssl rand -hex 32) ---
AUTHELIA_SESSION_SECRET=change_me
AUTHELIA_STORAGE_ENCRYPTION_KEY=change_me

# --- Tunnel Cloudflare ---
# Token du tunnel (cloudflared). À récupérer sur dash.cloudflare.com.
CLOUDFLARE_TUNNEL_TOKEN=change_me

# --- BrightBean Studio (social media management) ---
# Générés par : openssl rand -hex 32 (SECRET_KEY) / -hex 16 (les deux autres)
BRIGHTBEAN_SECRET_KEY=change_me
BRIGHTBEAN_ENCRYPTION_KEY_SALT=change_me
BRIGHTBEAN_POSTGRES_PASSWORD=change_me
